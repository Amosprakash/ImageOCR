# ImageOCR Environment Configuration
# Copy this file to .env and fill in your actual values

# === Server Configuration ===
# Host address for the FastAPI server (default: 0.0.0.0 for all interfaces)
HOST=0.0.0.0

# Port for the FastAPI server (default: 3000)
PORT=3000

# Enable auto-reload for development (default: True)
RELOAD=True

# === CORS Configuration ===
# Comma-separated list of allowed origins for CORS
# Use "*" for development only - specify exact domains for production
# Example: ALLOWED_ORIGINS=https://yourdomain.com,https://app.yourdomain.com
ALLOWED_ORIGINS=*

# === OCR Configuration ===
# Path to Tesseract OCR executable
# Leave as "tesseract" if installed in system PATH
# Windows example: C:\Program Files\Tesseract-OCR\tesseract.exe
# Linux/Mac: Usually just "tesseract"
TESSERACT_PATH=tesseract

# Path to Poppler binaries for PDF processing (optional)
# Only needed if PDFs require image conversion
# Windows example: C:\Program Files\poppler-25.07.0\Library\bin
# Linux: Usually available in system PATH
POPPLER_PATH=

# === OpenAI Configuration ===
# OpenAI API key for GPT integration
# Required for structured text extraction and question answering
OPENAI_API_KEY=your_openai_api_key_here

# === Google Vision API (Optional) ===
# Path to Google Cloud credentials JSON file
# Only needed if using Google Vision API
GOOGLE_APPLICATION_CREDENTIALS=
